import 'package:flutter/material.dart';

class SuperAdminAnaliticasScreen extends StatelessWidget {
  const SuperAdminAnaliticasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Análisis (pendiente)"));
  }
}
