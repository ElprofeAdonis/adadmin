import 'package:flutter/material.dart';

class SuperAdminUsuariosScreen extends StatelessWidget {
  const SuperAdminUsuariosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Listado de usuarios (pendiente)"));
  }
}
